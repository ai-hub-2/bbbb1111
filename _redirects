# Cloudflare Pages Redirects
# إعادة توجيه Cloudflare Pages

# إعادة توجيه الصفحات (أولاً للأداء الأفضل)
/dashboard    /    200
/login       /    200
/register    /    200

# إعادة توجيه API calls
/api/*    /api/:splat    200

# إعادة توجيه جميع الطلبات الأخرى إلى التطبيق الرئيسي
/*    /index.html   200